-- Go to the repo root: claude-certified-architect/ and start claude --
--

Build Lab 04 following the authoring instructions.
Read these files first, in this order:

authoring/AUTHOR.md — authoring conventions and how to build a lab
authoring/CCA_Exam_Guide.md — read the task statements listed in the Lab 04 plan
authoring/CCA_Lab_Reference.md — cross-reference task coverage
authoring/CCA_Lab_Plan.md — find the Lab 04 section for scenario, platform, domains, files, and key concepts
01_customer_support_agent/ — reference only for: README progression, file layout, interactive menus, ANSI colors. Do not copy SDK or tool patterns from this lab — it uses the wrong SDK.
02_code_generation_workflows/ — reference only for: README step structure and configuration-oriented conventions. Do not copy SDK or tool patterns unless this lab's platform matches.
03_multi_agent_research/ — reference only for: README structure, file layout, and conventions. Do not copy SDK or tool patterns unless this lab's platform matches.

Determine the correct SDK before writing any code:
Check the Platform field in CCA_Lab_Plan.md for Lab 04, then follow exactly one path:

Claude Agent SDK → pip install claude-agent-sdk, from claude_agent_sdk import query, ClaudeAgentOptions, AgentDefinition, HookMatcher. Do NOT use import anthropic or manual agentic loops. Read the SDK docs first: https://docs.anthropic.com/en/docs/claude-code/sdk — study patterns for query(), AgentDefinition, HookMatcher, and mcp_servers.
Claude API → pip install anthropic, import anthropic, manual agentic loops, _schema dict tool definitions. Reference: https://docs.anthropic.com/en/api/client-sdks.
Claude Code → no SDK needed. The lab configures Claude Code, not code tools.

Output:
Produce all files in a new 04_<scenario_name>/ folder: README.md and all supporting files, following the structure and conventions from the completed labs above.
Hard constraints:

Use the Read tool for all file reading
Do not use git log, git show, or git diff
Do not read authoring/reference_lab/ — it is outdated and superseded
Do not read files outside authoring/, 01_customer_support_agent/, 02_code_generation_workflows/, and 03_multi_agent_research/ unless explicitly directed above